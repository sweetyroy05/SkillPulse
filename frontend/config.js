// SKILL PULSE — Frontend Runtime Configuration
// This file is the ONLY place you need to change the backend URL for deployment.
// Local development: keep as http://127.0.0.1:8000
// Production (Render): replace with your Render URL, e.g., https://YOUR-BACKEND.onrender.com
// Do NOT add trailing slash.

window.API_BASE_URL = "http://127.0.0.1:8000";

// Alternative (Vercel env var injection is not available for plain HTML/JS):
// If you prefer, you can keep this file as localhost and manually edit it after you get your Render URL,
// then redeploy frontend to Vercel. No other code needs changing.
